version: 2

build:
  os: "ubuntu-20.04"
  tools:
    python: "3.7"

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs
        - qt
        - all

formats: []

sphinx:
  fail_on_warning: true
